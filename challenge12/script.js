// Données 1

let tempMaxs = [17, 21, 23]

tempMaxs.forEach((tempMax, index) => {
    console.log(`${tempMax} degrés dans ${index + 1} jour${index + 1 > 1 ? 's' : ''}`);
})

// Données 2

tempMaxs = [12, 5, -5, 0, 4]

tempMaxs.forEach((tempMax, index) => {
    console.log(`${tempMax} degrés dans ${index + 1} jour${index + 1 > 1 ? 's' : ''}`);
})